node_modules
.env
.env.*
*.env
!**/*.env.example
database.sqlite
.claude/

# Local reference material — kept on disk for design/photo lookups but not
# tracked. The design tokens that the app actually uses live in
# frontend/src/styles/ak-tokens.css.
Mafiozo Design System/
photos/
